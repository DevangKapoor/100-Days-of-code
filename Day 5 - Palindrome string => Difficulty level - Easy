class Solution{
public:	
	
	
	int isPalindrome(string S)
	{
	    string sp="";
	    for(int i=S.length()-1;i>-1;i--)
	    {
	        sp+=S[i];
	    }
	    if(sp==S)
	        return 1;
	    return 0;
	    
	}

};
